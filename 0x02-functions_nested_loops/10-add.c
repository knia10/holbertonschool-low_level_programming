#include "holberton.h"

/**
 * add - Write a function that adds two integers and returns the result
 * @a: one argument
 * @b: two arguments
 * Return: add of both of them arguments
 */

int add(int num1, int num2)
{
	return (num1 + num2);
}
